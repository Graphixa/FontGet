// Package cmdutils contains CLI-facing helpers used by `cmd/*`.
//
// It holds Cobra/Elevation/Path helpers and user-facing error formatting so command
// implementations can stay thin without pulling CLI concerns into lower-level packages.
package cmdutils
