// Package functions contains feature/domain-specific helpers.
//
// Prefer this package when helpers are tightly coupled to specific domain types (e.g. sources
// management). For CLI-agnostic, broadly reusable utilities, prefer `internal/shared`.
package functions
